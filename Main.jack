// Author:    Daniel Edwards
// File name: Main.jack

/** Initializes a maze game and starts running it. */
class Main {
    function void main() {
        var MazeGame game;
        let game = MazeGame.new();
        do game.run();
        do game.dispose();
        return;
    }
}
